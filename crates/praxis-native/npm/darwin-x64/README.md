# @plures/praxis-native-darwin-x64

Platform-specific native binary for `@plures/praxis-native`.

This package is installed automatically as an optional dependency. You do not need to install it directly.
