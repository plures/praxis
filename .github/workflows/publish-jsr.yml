name: Publish to JSR

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish:
    name: Publish to JSR
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      id-token: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: v1.x
      
      # TODO: Publish to JSR when package is ready
      # - name: Publish to JSR
      #   run: deno publish --allow-dirty
      
      - name: Placeholder - JSR Publishing Not Yet Configured
        run: |
          echo "JSR publishing workflow is ready but not yet enabled"
          echo "To enable, uncomment the publish step and configure JSR metadata"
          echo "See: https://jsr.io/docs/publishing-packages"
